# Benchmarks

Benchmark scripts and results will live here.

The target benchmark tool is `wrk`. Every result should record:

- hardware and operating system;
- compiler and build type;
- server configuration;
- command line;
- throughput;
- p50, p95, and p99 latency;
- non-2xx/3xx responses and socket errors.

